

-- SQL Data Types

-- String Data types
-- char,varchar,nchar,nvarchar

-- M or F, char(1)
-- varchar(256)- you can put any limit
-- nchar (1) - it support unicode
-- nvarchar(256) - it support unicode as well

-- Number Data types
-- int,bigint,tinyint,decimal(p,s), smallint

-- age of the student -tinyint (we can use int as well but space of tinyint is less)
-- age of the building/tre - smallint
-- decimal(p,s) - p represents total digits , s refers to number of digits you may have after decimal
-- 999.99, decimal(5,2)

-- date data type, time data type, datetime data type